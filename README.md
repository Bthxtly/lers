# Lers
Rewrite Lex in Rust with extension.
Also this is my data structure course design.

## Usage
Run the command below to generate a "lex.yy.c" file, it should tokenize test
properly.

`cargo run -- lex.l`
